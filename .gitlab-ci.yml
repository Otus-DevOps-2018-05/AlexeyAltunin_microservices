image: ruby:2.4.2

stages:
  - build
  - test
  - deploy

variables:
  DATABASE_URL: 'mongodb://mongo/user_posts'

before_script:
  - cd reddit
  - bundle install


build_job:
  stage: build
  tags:
    - docker
  script:
    - echo 'Building'

test_unit_job:
  stage: test
  tags:
    - docker
  script:
    - echo 'Testing 1'

test_integration_job:
  stage: test
  services:
    - mongo:latest
  script:
    - ruby simpletest.rb
  tags:
    - docker
  script:
    - echo 'Testing 2'

deploy_job:
  stage: deploy
  tags:
    - docker
  script:
    - echo 'Deploy'
